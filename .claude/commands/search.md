# /search — Semantic Search
# Invoke: /search <query>

Search memory, plans, facts, and code semantically.

## Usage

```
/search "how did I fix auth rate-limit"
/search "best React state management"
/search "previous plan for login system"
```

## Powered by

`smart-memory` skill — uses vector search (sqlite-vec) for semantic matching.
